import React from "react"

const button = (props) => {
    return(
        <button className="butao" className="button-sorteador" onClick={props.click}>SORTEAR</button>
    );
}

export default button