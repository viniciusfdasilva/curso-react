import React from "react";

const mega = (props) =>{
    
    return(
        <div>
            <h1>O número sorteador é {props.numero}</h1>
            <h3>Digite a quantidade de numeros a ser gerado</h3>
        </div>
    );
}

export default mega